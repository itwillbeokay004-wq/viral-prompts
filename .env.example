# Optional: enable AI-powered prompt generation.
# Without this, the app serves from the curated static library.
OPENAI_API_KEY=

# Optional: override the default OpenAI model (defaults to gpt-4o-mini).
OPENAI_MODEL=gpt-4o-mini
